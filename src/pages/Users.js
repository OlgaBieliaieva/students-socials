import Header from 'components/Header/Header';

const pageStyles = {
  display: 'flex',
  width: '100%',
  height: '100vh',
};

export const Users = () => {
  return (
    <main style={pageStyles}>
      <Header />
    </main>
  );
};
